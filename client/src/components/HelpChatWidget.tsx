/**
 * Legacy help chat — UI removed to avoid duplicate assistant FABs.
 * Footer / contact links dispatch `spacilly-open-help-chat`, handled by AssistantChat.
 */
export default function HelpChatWidget() {
  return null;
}
