/** Site-wide body font (see theme.css --font-body). */
export const FONT_BODY = "'Times New Roman', Times, serif";

/** Reaglex logo wordmark only — not for sentences that mention Reaglex. */
export const FONT_LOGO = "'Mea Culpa', serif";
