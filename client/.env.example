# API Configuration
# Backend API base URL (include /api if your backend serves API under /api)
VITE_API_URL=http://localhost:5000/api

# Server URL (for WebSocket and file uploads)
# Usually the same as API URL but without /api
VITE_SERVER_URL=http://localhost:5000

# WebSocket URL (optional, defaults to VITE_SERVER_URL)
# VITE_WS_URL=http://localhost:5000
